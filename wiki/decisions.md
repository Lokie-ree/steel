# Decisions

Append-only architecture decision log for the ecosystem. Gap this file covers that no existing doc does: cross-repo rulings had no durable home — they lived in conversation memory and spoke-local docs, invisible at the hub. One entry per ruling; never edit past entries, append corrections as new entries.

| Date | Ruling | Why | Where it binds |
|------|--------|-----|----------------|
| 2026-07-24 | **course-lab stays client-side — NOT-DOING a backend.** Persistence remains `localStorage` with the in-memory fallback ([[patterns]] §6). This is not a maturity gap versus project-studio-coach's Convex: that stack was *required* there — RAG needs a server, and plan-gating is enforced at the query projection, which only exists server-side. course-lab has no server-side logic to protect. Staying client-side buys three things a backend would spend: no district data agreement, no auth surface, no PII leaving the Chromebook. **Two named triggers reopen this:** (a) collection fails in a real class period, or (b) a second teacher adopts course-lab. Absent either, a backend is scope, not progress. | The two repos get compared on stack sophistication and the comparison is wrong — studio-coach's server is load-bearing for its own reasons, and copying it into course-lab would import compliance cost to solve a problem course-lab does not have. Naming the triggers means the decision can be revisited on evidence instead of on unease. | [[../projects/course-lab]] · [[patterns]] §6 · [[depth-criteria]] |
| 2026-07-22 | **R3F has no fall surface; creative-lab stays a live spoke with no fall build.** The 2026–27 assignment shrank from six preps to two (Algebra II/III on EdgeEx). Neither course's scope & sequence carries spatial content — creative-lab M1–M3 are Geometry, prerequisite territory ([[edge-ex-courses]] §Fit with existing builds). So no fall build targets R3F/CSG. creative-lab is **not** dormant-by-neglect: it remains the registered R3F home and a shipping capability, available the moment a genuinely spatial build earns a slot. | Absence of a fall build is a scope ruling, not decay — writing it down stops the next session from either "reviving" creative-lab out of guilt or quietly closing a working spoke. | [[../projects/creative-lab]] · [[../initiatives/edgeex-build-family]] · [[../sprint/2026-fall-runway]] |
| 2026-07-22 | **Fewer, deeper over breadth — and the first student data is a gate.** Two preps instead of six frees the course-lab infrastructure (sink-hardening, registry guard, roster, telemetry, live deploy) that was built for the larger workload. That slack goes into depth on a small slate, not into more builds. Build one is **transformations-ptr**; build two does not start until build one has run with real students and cleared the depth criteria (P2, `wiki/` — round-2 miss rate, reconcile quality, commit-before-reveal comparison, producer retry/ghost-path counts). | Infrastructure sized for six preps is slack, not waste; spending it on feature count would reproduce the breadth problem the shrink just removed. A gate keyed to student data is the only honest check that a build teaches. | [[../initiatives/edgeex-build-family]] · [[../sprint/2026-fall-runway]] · `course-lab/docs/superpowers/plans/PLAN-course-lab-transformations-ptr.md` |
| 2026-07-19 | **transformations-ptr lives in course-lab, not creative-lab.** The EdgeEx build family's first module is a 2D SVG function-graph tool — no R3F content, so the stack boundary cuts for course-lab; production room, live URL, StartGate/roster/telemetry already verified there. creative-lab remains the R3F home; "adapt a creative-lab module" stays a legal option for genuinely spatial builds. | A dormant exhibit repo with no student gate shouldn't grow the production room's fall content; the boundary rule is about the stack, not the sprint's initial guess. | `docs/superpowers/specs/2026-07-19-transformations-ptr-design.md` · [[../initiatives/edgeex-build-family]] · [[../projects/course-lab]] |
| 2026-07-18 | **AI-for-teachers lineage consolidated (portfolio consolidation, 5 rulings).** aida/Pelican AI: **absorb the spine** — Pelican-as-product closes for now; its RAG/ingestion/agent architecture becomes the canonical pattern for teacher-facing AI, the Louisiana corpus stays revivable. edcoachai, sped-sync, pelican-ai-strategy: **close** (harvested). ts-google-automation: **absorb** (markdown→Forms parser pattern). | The lineage's value is its architecture and corpus, not four dormant products competing with the 2026 classroom for attention; one ruling ends the ambiguity. | [[../archaeology/index]] · [[../archaeology/aida]] · [[patterns]] |
| 2026-07-18 | **Portfolio identity: the ground-up `portfolio` spoke stays the door.** It is the operator's own vision and the URL on the business cards; the surface evolves from there. portfolio-markdown-site and sanity-ai-portfolio: **absorb then close** — harvest markdown-sync, append-only analytics, AEO/llms.txt surface, ChatKit session-gating, guardrail prompts; the forked codebases are not the identity. stoic-scholar: **close, folder deleted** (was empty). | Identity beats recency: a portfolio built from someone else's fork can't be the professional face, however good its concepts. | [[../archaeology/portfolio]] · [[../archaeology/portfolio-markdown-site]] · [[../archaeology/sanity-ai-portfolio]] |
| 2026-07-18 | **Foundations/support closes:** coding-playground-vanilla and iste-outreach-tracker **close** (spent, single-purpose); github-readme **absorb** — stays live as the canonical public constellation map feeding [[journey]]. | Finished tools whose only remaining value is harvested patterns and narrative. | [[../archaeology/index]] |
| 2026-07-17 | **STEEL is a workflow-governance hub; project-studio-coach admitted as sixth spoke.** The hub governs development workflow (git conventions, session briefs, drift/state visibility) for any actively-developed project the operator registers; the ISTE arc is one cluster among spokes, not the hub's definition. studio-coach's 13 pre-admission commits (1 bootstrap + 12 build) are grandfathered — conventions apply from admission forward. | Admitting a non-ISTE, non-Creative-Lab platform forced the identity question; deciding it once beats re-litigating per project. | [[../projects/project-studio-coach]] · [[../index]] Purpose line · `docs/superpowers/specs/2026-07-17-studio-coach-onboarding-design.md` |
| 2026-07-06 | **course-lab is a new spoke repo** — not housed in creative-lab-demos (identity is ISTE demos, this is production) and not in creative-lab (its conventions are R3F-shaped). | The two rendering stacks stay separate at the repo boundary: R3F/GSAP in creative-lab, plain JSX/SVG in course-lab. Hard rule: `three` / `@react-three/*` / `gsap` never appear in course-lab's `package.json`. | [[../projects/course-lab]] · founding spec §2–3 (`course-lab/docs/course-lab-founding-spec.md`) |
| 2026-05-30 | **M2 and M3 intentionally share the same canonical triangle** — A(1,1) B(4,2) C(2,4). Not drift; the shared object lets M3 reuse a triangle the student has already met in M2, then reveal a new property (the Pythagorean relationship) on familiar geometry. | `HubContext`'s parser and test suite (`ops/tests/HubContext.Tests.ps1`) treat M2≡M3 as intentional and would otherwise flag it as a Tier 1 mismatch. | [[../sync-registry]] · [[../wiki/module-facts]] |
